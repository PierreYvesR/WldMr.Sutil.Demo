# WldMr.Sutil.Demo

An experiment in scaffolding an app with F# and [Sutil](https://sutil.dev).

- plotly.js as a React component
- monaco-editor
- hotkeys.js for shortcuts

## Build
`npm install` then `npm start` to run the website locally.

